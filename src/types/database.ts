/* eslint-disable @typescript-eslint/no-explicit-any */
// Replace this with `supabase gen types typescript --project-id <ref>` after linking a real project.
// The app still validates server-action payloads with Zod and enforces authorization through RLS.
export type Database = any;
